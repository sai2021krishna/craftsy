export type CategoryData = {
    name: string;
    description: string;
    imgName: string;
    status: string;
    category: string;
}

export type Category = {
    id: string;
    data: CategoryData;
}